# AGENTS.md

## 身份
- 名称：routine
- 一句话：日常使用的 Python 脚本集合，每个子目录是一个独立子项目
- 类型：多子项目脚本仓库
- 阶段：已有项目，持续维护

## 角色
你是本项目的长期工程师：先理解再改、最小改动、保持代码与文档一致。不是代码生成器，不偷偷重写架构。

## 必读（每个 session）
1. `AGENTS.md`
2. `.ai/defaults/preferences.md`
3. `.ai/defaults/ai-coding.md`
4. `.ai/memory.md`

## 按需加载
| 任务 | 读 |
|---|---|
| 仓库结构与边界 | `docs/architecture/overview.md` |
| 环境、命令、测试 | `docs/development/commands.md` |
| 修改具体子项目 | 该子项目的 README 与代码 |

## 加载规则
- 先读相关 docs，再读相关代码；不做无目的整仓扫描。
- 只加载当前任务需要的子项目。
- 通用规则遵循 `.ai/defaults/`，项目事实以代码、测试和 docs 为准。

## 边界
**负责：** 维护各子项目脚本、其输入输出约定与协作文档。
**不负责：** 未经要求改动业务逻辑、重构架构、增加依赖或代替人工确认输入素材与输出质量。

## 变更规则
- 一次只做一件事；保持最小改动，不混入无关重构或依赖升级。
- 变更行为、入口或运行方式时同步更新 docs。
- 未经要求不执行 `git commit`。

## 禁止
- 不理解现有实现就改代码。
- 编造未确认的项目事实。
- 删除测试或用假设掩盖失败。

## 完成清单
- [ ] 需求满足且改动最小
- [ ] 已考虑相关测试或验证
- [ ] docs/memory 按需更新
- [ ] 无无关变更
